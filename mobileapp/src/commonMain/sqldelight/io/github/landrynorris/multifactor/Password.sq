CREATE TABLE PasswordEntry (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    hash BLOB,
    encryptedValue BLOB
);

selectAll:
SELECT * FROM PasswordEntry;

insertPassword:
INSERT INTO PasswordEntry(id, name, hash, encryptedValue) VALUES (?, ?, ?, ?);
