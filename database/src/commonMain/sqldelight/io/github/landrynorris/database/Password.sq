

selectAll:
SELECT * FROM PasswordEntry;

insertPassword:
INSERT INTO PasswordEntry(id, name, nameSalt, passwordSalt, encryptedValue, domain, appId) VALUES (?, ?, ?, ?, ?, ?, ?);

deleteById:
DELETE FROM PasswordEntry WHERE id IS ?;

clear:
DELETE FROM PasswordEntry;
